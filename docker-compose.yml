version: "3"

services:
  rz-comment-api:
    image: rinz-comment
    build: .
    restart: always
    volumes:
      - ./:/webapps
    ports:
      - 6789:5005
    command: supervisord -n -c /etc/supervisor.d/service.conf
